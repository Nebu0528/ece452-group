# Project Titans Week 6 Update

June 8 - June 15
- [Jerry] setup project starter code and database implementation
  - Set up Postgres database with Python seed script and Alembic migration management
  - Created initial tables and specs for users, events, tags tables
  - Added default CRUD operations in FastAPI for database tables
  - Implemented basic authentication tokens for users
- [Sadiq] implemented navigation shell, auth screens, and data layer
  - Added email-based login/register with @uwaterloo.ca validation
  - Set up Room DB, Retrofit client, AppViewModel with API calls
  - Navigation with role-based bottom nav (organizer/admin tabs only shown when applicable)
  - UW gold/black theme applied
  - Backend updated to use email field on User model (migration 0002)
-[Minh] Implemented Home page
    - Display all Event Categories (Tags) on the top of the UI, which are able to toggle
    - Display Events as a lazy list, based on the above category filter
    - on select, went into details of the event
  - [Neel] implemented the events detail and planner page
    - planner page shows events that the user has signed up for 
    - the user can view the details in the events such as location, time, club etc...
    - user can remove events from planner
- [Rohan] implemented the map screen and location services
    - Integrated the OSMDroid map view into Jetpack Compose using the AndroidView wrapper.
    - Set up map overlays to dynamically plot event pins using latitude and longitude coordinates retrieved from the backend database/API.
    - Configured interactive marker info bubbles to display the event name and date/location popup upon tapping a pin.
    - Added coarse and fine location permissions to the Android manifest, defaulting the map view's starting focus on the UWaterloo campus.
  
